import { ComingSoonView } from "@/views/coming-soon";

export default function SessionOnePage() {
  return (
    <ComingSoonView
      content={{
        eyebrow: "Session I",
        title: "First Contact",
        body: "The first session recap, in the same chronicle format as the Sundering myths — still being transcribed by the archive-keepers.",
      }}
    />
  );
}
