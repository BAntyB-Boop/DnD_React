import { ComingSoonView } from "@/views/coming-soon";

export default function OneshotNovaArrivalPage() {
  return (
    <ComingSoonView
      content={{
        eyebrow: "One-Shot",
        title: "Arrival at Nova Stellare",
        body: "A full adventure module for the DM's table — read progress bar, hero title, and all. Still being bound into the Codex.",
      }}
    />
  );
}
