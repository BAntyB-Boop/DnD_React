import { ComingSoonView } from "@/views/coming-soon";

export default function MapNovaStellarePage() {
  return (
    <ComingSoonView
      content={{
        eyebrow: "The Drift",
        title: "Map of the Drift",
        body: "An interactive star-chart with waystation hotspots and DM notes — the cartography is still being drawn.",
      }}
    />
  );
}
